<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href = "/user_home.css">
    <title>Document</title>
    
</head>
<body>
    <h1>user page </h1>
    <a href = '/'> go to home </a>

     <div id = "rented_homes">
        <ul>
           <% if (registeredHome && registeredHome.length > 0) { %>
                   <% registeredHome.forEach(element => { %>
                  <li><%= element.name %>, <%= element.price %></li>
           <% }); %>
           <% } else { %>
                  <li>No homes rented yet</li>
           <% } %>
     
        </ul>
    </div>
</body>
</html>